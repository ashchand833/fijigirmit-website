UPLOAD INSTRUCTIONS

Upload/replace these in the ROOT of the GitHub staging branch:
- index.html
- about.html
- history.html
- events.html
- contact.html
- donate.html
- site-content.js
- site-style.css
- config.yml

Upload/replace this inside the ADMIN folder:
- admin/config.yml

Upload this image inside the IMAGES folder:
- images/about-girmit-elders-banner.jpg

This version locks the global header using one shared site-style.css file, so every page uses the exact same logo size, title font, nav position, header height, hover style, and Donate button style.

Events are loaded from the /events admin markdown files and sorted by Event Date newest/future first. Cards use Event Flyer / Cover Image first, then Event Photos.

Admin rule:
For an event cover/card image, use Event Flyer / Cover Image. Use Event Photos only for extra gallery photos. Best image size: 1600x900 landscape.
